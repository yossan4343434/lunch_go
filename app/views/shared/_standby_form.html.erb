<%= form_for(@standby) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.date_field :date %>
  </div>
  <%= f.submit "ランチGO!の予約をする", class: "btn btn-primary" %>
<% end %>
