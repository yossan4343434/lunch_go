<% if logged_in? %>
  <div class="row">
    <aside class ="col-md-4">
      <section class="standby_form">
        <%= render 'shared/standby_form' %>
      </section>
      <section class="match_form">
        <%= render 'shared/match_form' %>
      </section>
    </aside>
    <div class="col-md-8">
      <section class="todays_match">
        <%= render 'shared/todays_match' %>
      </section>
      <section class="standby_reservations">
        <%= render 'shared/standby_reservations' %>
      </section>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>ランチGO ! へようこそ !</h1>
    <%= link_to "今すぐ登録", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
